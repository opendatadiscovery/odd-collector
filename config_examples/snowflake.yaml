platform_host_url: http://localhost:8080
default_pulling_interval: 10
token: ""
plugins:
  - type: snowflake
    name: snowflake_adapter
    host: account.snowflakecomputing.com
    port: # Optional
    user: user
    password: password
    database: database 
    account: account # Optional account locator, if omit, will be taken from host <account>.snowflakecomputing.com
    warehouse: warehouse # i.e COMPUTE_WH