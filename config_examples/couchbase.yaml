platform_host_url: http://localhost:8080
default_pulling_interval: 10
token: ""
plugins:
  - type: couchbase
    name: couchbase_adapter
    description: "" # Optional string
    bucket: bucket
    host: host # Connection string
    user: user
    password: password
    sample_size: 0 # change to 1 to get sample if you need check type manually
    num_sample_values: 10 # get collection info based on random N documents