platform_host_url: http://localhost:8080
default_pulling_interval: 10
token: ""
plugins:
  - type: presto
    name: presto_adapter
    description: "" # Optional string
    host: localhost
    port: 8081
    user: root
    password: "" # Optional string (LDAP-configured cluster)
    principal_id: "" # Optional string (LDAP-configured cluster)